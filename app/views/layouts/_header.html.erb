<nav class="navbar navbar-toggleable-md navbar-inverse bg-inverse">
  <div class="container">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <%= link_to 'Address book', root_path, class: "navbar-brand" %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          <li class="nav-item <%= 'active' if current_page?(new_contact_path) %>">
            <%= link_to 'New Contact', new_contact_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
      <% if user_signed_in? %>
        <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-outline-info my-2 my-sm-0" %>
      <% else %>
        <%= link_to "Sign in", user_session_path, class: "btn btn-outline-success my-2 my-sm-0 move-over" %>
        <%= link_to "Sign up", new_user_registration_path, class: "btn btn-outline-info my-2 my-sm-0" %>
      <% end %>
    </div>
  </div>
</nav>
